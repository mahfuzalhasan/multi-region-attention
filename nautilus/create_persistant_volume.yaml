apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: segmentation-vol
spec:
  storageClassName: seaweedfs-storage-nvme
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
